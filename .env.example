DATABASE_URL="postgresql://user:password@host:5432/dbname?schema=public"
KNOWLEDGE_API_KEY=""

# Optional. Only set this in Railway's Staging environment Variables tab.
# When unset, middleware.ts is a no-op and every request passes through
# unchanged — this is what keeps production and local dev (this file)
# ungated by default.
STAGING_BASIC_AUTH_PASSWORD=""
